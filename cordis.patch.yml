# Agent Observability Plugin — bundle patch layer
- insert:
    - id: agent-observe
      name: dsh-agent-observe
      inject: [tools, sessionProjections, llm, agentDefaultModel, webServer]
      config:
        retentionDays: 90
        maxSessions: 10000